# 春哥空间 CHUNGE.SPACE — 项目交接文档

> 本文件夹是「春哥空间」项目的完整源码，供后续接手（dumate）继续开发 / 部署使用。

---

## 1. 项目是什么

- **品牌**：春哥空间 CHUNGE.SPACE，域名 `chunge.space`
- **定位**：一个普通人的长期创业实验（个人博客 + 项目展示 + 评论互动）
- **作者**：李长春（春哥），做了 8 年保险，组织发展 / 招募背景，现重启个人产能

## 2. 技术栈

| 层 | 选型 |
|---|---|
| 框架 | Next.js 15（App Router）+ React 19 + TypeScript |
| 样式 | Tailwind CSS v4（`@tailwindcss/postcss`） |
| 数据库 | Prisma 6 + SQLite（本地开发）/ Cloudflare D1（生产，SQLite 兼容） |
| 部署 | Cloudflare Pages（`@cloudflare/next-on-pages` + `wrangler`） |
| 鉴权 | HMAC 轻量 token（localStorage 存储，演示级，非生产强鉴权） |

## 3. 当前状态（截至交接时）

- ✅ 代码完成，`next build` 通过（首页静态 + 3 个 API 动态）
- ✅ 本地冒烟测试全绿：注册 201 / 登录 200 / 错密 401 / 评论 201 / 嵌套回复 201 / 游客拦截 401
- ✅ Cloudflare D1 改造完成：`src/lib/prisma.ts` 的 `getPrisma()` 自动切换本地 SQLite 与边缘 D1，**schema 零改动**
- ⚠️ 尚未实际部署到 Cloudflare（以下步骤待执行，属用户侧操作）

## 4. Ardot 设计稿（云端，需 WorkBuddy 打开）

设计稿存于 Ardot 云端，无法作为本地文件，需用 WorkBuddy 按文件 ID 打开：

- **首页设计**：文件 ID `714526209092543`
- **注册 / 评论 / emoji 头像设计**：文件 ID `714530953207681`

打开方式：在 WorkBuddy 中打开对应 Ardot 文件 ID 即可查看 / 继续编辑视觉稿。

## 5. 部署到 Cloudflare 的步骤

1. **建 D1 数据库**：`npx wrangler d1 create chunge-space-db`，把返回的 `id` 填入 `wrangler.toml` 的 `database_id`（当前为占位符 `REPLACE_WITH_YOUR_D1_DATABASE_ID`）
2. **设环境变量**：在 Cloudflare Pages 项目设置里添加 `AUTH_SECRET`，用强随机值（本地默认值仅用于开发）
3. **安装依赖并部署**：`npm install` → `npm run cf:deploy`（会依次执行 build → pages:build → pages:deploy）
4. **绑定域名**：在 Pages 设置里添加 `chunge.space`，按提示到域名商处配 CNAME
5. **等 SSL**：通常几分钟自动签发

## 6. 首次部署后必须建表

`wrangler.toml` 指向的 D1 初始为空，需把 Prisma schema 同步过去：

```bash
npm run db:d1:sql          # 生成 prisma/init.sql（建表语句）
npm run db:d1:push:remote  # 推送到线上 D1
```

## 7. 文件结构速览

```
chunge-space/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   ├── register/route.ts   # 注册（昵称/手机号/密码/性别/emoji 头像）
│   │   │   ├── login/route.ts      # 登录 + 会话校验
│   │   │   └── comments/route.ts   # 评论 + 嵌套回复（需登录）
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   └── page.tsx                # 首页 8 模块组装
│   ├── components/                 # Header/Hero/About/NowDoing/RecentThoughts/
│   │                               # Practice/DailyLog/Timeline/Statement/Welcome/Footer
│   │                               # + 交互层：AuthProvider/AuthModal/CommentSection/
│   │                               # EmojiAvatarPicker/AuthModalProvider
│   ├── data/content.ts             # 首页文案 / 时间线 / 日志内容
│   └── lib/
│       ├── auth.ts                 # HMAC token 生成 / 校验
│       ├── prisma.ts               # getPrisma() 本地/D1 自动切换（关键改造点）
│       └── types.ts
├── prisma/
│   ├── schema.prisma               # User / Comment 模型（SQLite 兼容 D1）
│   └── dev.db                      # 本地 SQLite 示例库（生产用 D1，可忽略）
├── .env                            # 本地开发环境变量（见第 8 节）
├── wrangler.toml                   # Cloudflare Pages + D1 绑定配置
├── package.json                    # 依赖与脚本（见下）
└── next.config.mjs / tsconfig.json / postcss.config.mjs
```

### package.json 关键脚本

| 脚本 | 作用 |
|---|---|
| `npm run dev` | 本地开发 |
| `npm run build` | `prisma generate && next build` |
| `npm run cf:deploy` | build → pages:build → pages:deploy |
| `npm run db:d1:sql` | 生成 D1 建表 SQL 到 `prisma/init.sql` |
| `npm run db:d1:push:remote` | 把建表 SQL 推到线上 D1 |

## 8. 注意事项

- **`.env` 含本地开发密钥** `AUTH_SECRET="chunge-space-dev-secret-change-me"`，仅用于本地；部署到 Cloudflare 时务必在 Pages 环境变量里设成强随机值，否则 token 可被伪造。
- **`DATABASE_URL` 仅本地用**：Cloudflare 环境由 `wrangler.toml` 的 D1 绑定（`binding = "DB"`）接管，`getPrisma()` 会自动走 D1 适配器。
- **`node_modules/`、`.next/`、`.vercel/` 未复制**：这些都可重建（`npm install` / `next build` / `pages:build`），不纳入交接以减小体积。
- **`prisma/dev.db`** 是本地 SQLite 示例数据，生产环境用 D1，可忽略或作为数据参考。

## 9. 待接手的潜在任务（可选）

- 实测 Cloudflare 部署全流程并验证线上评论 / 注册可用
- 替换 `src/components/Hero.tsx` 里的渐变占位框为真实封面图
- 可选：把界面整合回 Ardot 设计稿，便于后续视觉迭代
- 生产级鉴权加固（当前 HMAC token 为演示级）

---

**交接状态**：代码已 Cloudflare 就绪，用户按第 5、6 节操作即可上线。设计稿在 Ardot 云端（第 4 节 ID）。
